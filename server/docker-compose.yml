services:
  whatamidoing:
    build: .
    container_name: whatamidoing
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      # 必填：设备上报 token（与客户端配置一致）
      REPORT_TOKEN: ${REPORT_TOKEN:?请在 .env 或环境中设置 REPORT_TOKEN}
      # 可选：设置后查看网页需输入密码
      VIEWER_PASSWORD: ${VIEWER_PASSWORD:-}
      # 可选：离线判定阈值
      IDLE_TIMEOUT: ${IDLE_TIMEOUT:-30s}
