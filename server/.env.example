# whatamidoing 服务端环境变量
#
# 管理员密码、网页查看密码、设备 token 都在网页管理面板中管理，
# 并持久化到 DATA_FILE（默认 /data/data.json，Docker 挂载 ./data:/data）。
# 无需在这里配置凭据。

# 可选：首次初始化管理员的令牌（不设则随机生成并打印到启动日志）
# SETUP_TOKEN=

# 可选：离线判定阈值
IDLE_TIMEOUT=30s
