# whatamidoing 服务端环境变量
#
# 管理员密码、网页查看密码、设备 token 都在网页管理面板中管理，
# 并持久化到 DATA_FILE（默认 /data/data.json，Docker 挂载 ./data:/data）。
# 无需在这里配置凭据。

# 必填（首次初始化管理员）：初始化令牌。首次启动时需在网页输入此令牌设置管理员密码
SETUP_TOKEN=请改成一段随机字符串

# 可选：可信反向代理 IP/CIDR，逗号分隔。
# 部署在 Cloudflare / Nginx / Caddy 之后时填代理网段，限流才会正确识别访问者 IP；
# 不填则一律按直连 IP 限流。
# TRUSTED_PROXIES=173.245.48.0/20,103.21.244.0/22

# 可选：离线判定阈值
IDLE_TIMEOUT=30s
